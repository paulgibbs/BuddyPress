default:
  extensions:
    Behat\MinkExtension:
      base_url:           http://localhost:8000
      default_session:    no_js_session
      javascript_session: js_session
      sessions:
        no_js_session:
          goutte: ~
        js_session:
          selenium2: ~

imports:
  - ../../../../../../behat.local.yml
